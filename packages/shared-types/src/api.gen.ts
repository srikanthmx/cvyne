/**
 * AUTO-GENERATED — DO NOT EDIT BY HAND
 *
 * Regenerate with:
 *   1. cd apps/api && make dev   # start FastAPI
 *   2. cd packages/shared-types && pnpm generate
 *
 * This file is checked in so the frontend can build without the API running.
 * CI verifies it stays in sync.
 */

// Placeholder until first generation. After running `pnpm generate`,
// this file will contain the full OpenAPI-derived types.
export type paths = Record<string, never>;
export type components = { schemas: Record<string, never> };
